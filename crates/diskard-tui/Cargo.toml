[package]
name = "diskard-tui"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
description = "TUI frontend for diskard"
license = "MIT OR Apache-2.0"

[dependencies]
diskard-core = { path = "../diskard-core" }
ratatui = { version = "0.29", features = ["crossterm"] }
crossterm = "0.28"
